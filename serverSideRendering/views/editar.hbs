<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title></title>
    <style>
        div#root {
            width: 900px;
            margin: 50px auto;
        }
        form {
            display: flex;
            flex-direction: column;
        }
         form > * {
            margin: 10px 0;
        }
        footer p {
            text-align: center;
        }
        h1 {
            text-align: center;
        }
        table {
            margin: auto;
            background-color: #ffffff;
            font-size: 16px;
            border-collapse: collapse;
            margin-bottom: 1rem;
        }

        table tr:last-child td {
            border-bottom: 0;
        }

        table tr th,
        table tr td {
            text-align: left;
            padding: 1rem;
        }

        table tr th {
            color: #ffffff;
            background-color: #2f2f2f;
            border-bottom: solid 2px #d8d8d8;
            position: sticky;
            top: 0;
        }

        table tr td {
            border: solid 1px #d8d8d8;
            border-left: 0;
            border-right: 0;
        }

        table tbody tr {
            transition: background-color 150ms ease-out;
        }

        table tbody tr:nth-child(2n) {
            background-color: whitesmoke;
        }

        table tbody tr:hover {
            background-color: #ebebeb;
        }
        .x1{
            color: black;
            font-size: 40px;
            margin-right: 34px;
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <a class="x1" href="/"> Tabla de cursos</a></li>
                
                <a class="x1" href="/crear">Crear Curso</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <div id="root">    

<h2>editar curso</h2>
<form method="POST">
    <input type="hidden" name="verificacionID" value="{{_id}}"/>
    <label for="Curso">Curso</label>
    <input type="text" name="Curso" value="{{Curso}}" id="Curso" required/>

    <label for="aprobados">Aprobados</label>
    <input type="text" name="aprobados" value="{{aprobados}}" id="aprobados" required/>

    <label for="recuperacion">Recuperacion</label>
    <input type="number" name="recuperacion" value="{{recuperacion}}" id="recuperacion" required/>

    <label for="reprobado">Reprobado</label>
    <input type="number" name="reprobado" value="{{reprobado}}" id="reprobado" required/>

    <label for="Tutorias">Tutorias</label>
    <input type="number" name="Tutorias" value="{{Tutorias}}" id="Tutorias" required/>

    <input type="submit" value="Actualizar verificacion"/>
</form>

